# section-010 / capstone: Filesystem Creation, Mounting, and Disk/Process Forensics

QEMU VM for the LFCS course — formatting/mounting a raw disk, comparing usage across two pre-filled disks, and unmounting the disk backing a running process's executable.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c labs/section-010/capstone/lab-01
```
