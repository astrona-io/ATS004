# section-080 / module-03: XFS Quotas and Project Quotas

QEMU VM for the LFCS course — Setting XFS user block limits and defining a project quota that caps a directory tree regardless of file ownership.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c sections/section-080/module-03/labs/lab-01
```
