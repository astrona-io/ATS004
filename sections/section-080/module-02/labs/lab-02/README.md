# Hitting a Quota Hard Limit & the Grace Period

QEMU VM for the LFCS course — write past a user's hard quota limit and
confirm it's enforced, then observe the soft-limit grace-period countdown.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c sections/section-080/module-02/labs/lab-02
```
