# section-030 / capstone: LVM Volume Groups and Logical Volumes

QEMU VM for the LFCS course — reducing an LVM volume group with `pvmove`/`vgreduce` and building a new volume group, logical volume, and filesystem from the freed disk.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c labs/section-030/capstone/lab-01
```
