# Question

Solve this question on: `terminal`

Enable IPv4 packet routing for gateway capabilities.

1. Enable live IPv4 packet forwarding in the active kernel immediately without rebooting the system.
2. Persist this setting so that it remains active after a system reboot.
