# section-010 / module-02: Partitioning Raw Storage: GPT/MBR

QEMU VM for the LFCS course — Designing a modern GPT partition table on a raw block device and partitioning it safely with correct alignment using parted or fdisk.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c labs/section-010/module-02
```
