metadata:
  name: "ats-004-lab-022"
  docs:
    solution: "solution.md"
    question: "question.md"

runtime:
  type: "qemu"
  qemu:
  - image:
      type: "oci"
      source: "ghcr.io/astrona-io/ubuntu-qcow2-image:24.04-lfcs-{ARCH}"

    cpus: 2
    memoryMB: 2048
    diskSizeGB: 15
    display: false

bootstrap:
  init:
    - name: "lab-022-setup"
      description: "Installs NFS packages and populates exports"
      type: "folder"
      source: "bootstrap/"

validation:
  scripts:
    - name: "verify-nfs"
      description: "Confirms NFS export status and client mounting"
      type: "file"
      source: "validation/validate-completed.sh"

teardown:
  keepCluster: false
