metadata:
  name: "ats-004-lab-041"
  docs:
    solution: "solution.md"
    question: "question.md"

runtime:
  type: "qemu"
  qemu:
  - image:
      type: "oci"
      source: "ghcr.io/astrona-io/ubuntu-qcow2-image:24.04-lfcs-{ARCH}"

    cpus: 2
    memoryMB: 2048
    diskSizeGB: 15
    display: false

bootstrap:
  init:
    - name: "lab-041-setup"
      description: "Wipes any existing swap files"
      type: "folder"
      source: "bootstrap/"

validation:
  scripts:
    - name: "verify-swapfile"
      description: "Confirms swapfile creation, permissions, and persistent state"
      type: "file"
      source: "validation/validate-completed.sh"

teardown:
  keepCluster: false
