# section-050 / capstone: Filesystem Automount with autofs

QEMU VMs (`app-srv1` + `data-001`) for the LFCS course — configuring autofs on `app-srv1` to mount an NFS share from `data-001` on demand and release it after 5 minutes idle.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS004.git -c labs/section-050/capstone
```
